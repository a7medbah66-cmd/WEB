[package]
name = "engine_server"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine" }
axum = "0.7"         # Web framework
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"